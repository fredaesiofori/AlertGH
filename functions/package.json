{
  "name": "alertgh-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "~5.5.0",
    "@types/node": "^20.0.0"
  },
  "engines": { "node": "20" }
}
